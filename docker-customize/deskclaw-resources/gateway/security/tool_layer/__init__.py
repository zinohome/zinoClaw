"""Internal implementation for `ToolSecurityLayer` (policy, plugins, DLP, result pipeline, registry patch).

Public API remains `gateway.security.layer.ToolSecurityLayer`; do not import from here in application code.
"""
