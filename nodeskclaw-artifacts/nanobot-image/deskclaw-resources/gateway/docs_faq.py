"""User-facing FAQ documentation for the user_faq tool.

Written in plain language to help users understand how to use DeskClaw.
The agent retrieves these when users ask about usage, features, or
troubleshooting.
"""

from __future__ import annotations

FAQ: dict[str, str] = {}

FAQ["getting_started"] = """\
# 新手入门

## 首次启动
首次打开 DeskClaw 会自动完成环境准备（约 1-3 分钟），包括：
- 创建工作目录
- 安装运行环境（Python、Node.js）
- 安装预置技能（搜索、文档、浏览器等）
- 连接 AI 网关

进度条会实时显示当前步骤。如果某步骤失败，可以点击展开查看详情，或等待 AI 医生自动修复。

## 登录与激活
- 点击设置中的「登录」按钮，浏览器会打开登录页面
- 登录后输入邀请码激活，即可使用内置模型
- 也可以跳过登录，使用自定义 API（设置 → 自定义模式）

## 自定义 API 配置
1. 打开设置（左下角齿轮图标）
2. 切换到「自定义」模式
3. 填写 API 地址、密钥、模型名
4. 选择格式（OpenAI 兼容 或 Anthropic）
5. 点击测试连接，确认后保存

## 快捷操作
- **发送消息**: 输入框输入后按 Enter
- **换行**: Shift + Enter
- **新建对话**: 左侧栏「+ 新建任务」
- **停止回复**: 点击输入框旁的停止按钮
- **执行中追加指令**: 直接输入并回车，Bot 会自动合并处理
"""

FAQ["chat"] = """\
# 聊天功能

## 多会话
- 左侧栏可以看到所有会话，按时间分组
- 点击「+ 新建任务」创建新对话
- 右键会话可以重命名、置顶、删除
- 主会话（main）不能删除
- 切换会话时，当前输入和文件会自动保存为草稿

## 发送图片
- 直接粘贴剪贴板图片到输入框
- 支持多张图片同时发送
- 图片会显示上传预览，可点击删除
- 消息中的图片可以点击放大查看

## 工具审批
当 Bot 需要执行工具时，可能会弹出审批卡片：
- **执行**: 仅本次允许
- **始终允许**: 加入白名单，以后同类操作不再弹窗
- **拒绝**: 本次阻止执行

如果觉得弹窗太多，可以在设置 → 安全 → 开启「完全访问模式」跳过所有审批。

## 上下文用量
标题栏右侧的进度条显示当前对话的上下文使用比例。接近满时建议新建对话。
"""

FAQ["skills"] = """\
# 技能管理

## 查看技能
点击左侧栏「技能库」查看所有技能。每个技能有状态标识：
- ✅ 已就绪 — 可正常使用
- ⚙️ 需要配置 — 点击配置按钮填写必要信息（如 API Key）
- 🔄 有更新 — 可点击更新
- 📦 未安装 — 点击安装

## 安装技能
- 预置技能：直接点击「安装」按钮
- 第三方技能：通过 DeskHub 安装器技能搜索安装
- 安装后技能会自动同步到工作区

## 配置技能
部分技能需要额外配置（如 API Key、账号信息）：
1. 点击技能卡片上的配置按钮
2. 填写必要字段（密码字段可切换显示/隐藏）
3. 保存后可能需要重启生效

## 更新技能
- 单个更新：点击技能卡片上的更新按钮
- 全部更新：点击页面顶部的「全部更新」

## 内置技能说明
- **多引擎联网搜索**: 无需 API Key，集成 6 个国内搜索引擎
- **生图/图生图**: 支持多种尺寸，最多 14 张参考图
- **视频生成**: 支持 Kling、Seedance 模型
- **浏览器自动化**: 零配置，开箱即用
- **Office 三件套**: Word、Excel、PowerPoint 创建和编辑
- **B站助手**: 视频搜索/上传、动态发布、直播等
- **高德地图**: 地理编码、天气、路径规划
"""

FAQ["settings"] = """\
# 设置说明

## 打开设置
- 点击左下角齿轮图标
- 或通过系统托盘菜单 → 设置

## API 配置
### 默认模式
使用 DeskClaw 提供的云端模型，需要登录并激活邀请码。

### 自定义模式
连接自己的 API：
- **OpenAI 兼容格式**: 适用于大多数 API 服务（如 OpenAI、本地模型、中转站）
- **Anthropic 格式**: 适用于 Anthropic Claude API
- 填写 API 地址、密钥、模型名后点击「测试连接」验证

## MCP 服务器
MCP（Model Context Protocol）让 Bot 可以连接外部工具和服务。
- 内置的 `deskclaw` MCP 服务器不可删除
- 可以添加 stdio、SSE、streamableHttp 三种类型
- 修改后需要重启网关生效
- 你可以直接让 Bot 帮你添加 MCP 服务器（Bot 会使用专用工具安全配置，不会弄乱配置文件）
- 也可以在设置界面的 MCP 标签页中手动管理

## 安全设置
- **Bot 控制权限**: 允许 Bot 通过 MCP 修改沙箱模式、网络配置和循环守卫设置
- **完全访问模式**: 跳过所有工具审批弹窗
- **循环守卫**: 检测并阻止 Bot 陷入死循环（重复调用、连续失败）。灵敏度可选：
  - **保守**: 更早介入，适合高安全要求场景
  - **默认**: 平衡安全与灵活性
  - **宽松**: 更高容忍度，适合复杂长任务
- **命令白名单**: 设置可自动执行的终端命令
- **工具白名单**: 设置可自动放行的工具
- **沙箱模式**: 透明（直接执行）或隔离（容器内执行）

## 主题与语言
- 底部可切换主题（自动/浅色/深色）
- 底部可切换语言（中文/English）
"""

FAQ["cron"] = """\
# 定时任务

## 查看定时任务
点击左侧栏「定时任务」查看所有已创建的任务。

## 管理操作
- **启用/禁用**: 点击开关切换
- **删除**: 点击删除按钮，需确认
- **查看历史**: 点击查看该任务的所有执行记录（时间、状态、耗时、token 用量、错误信息）

## 如何创建定时任务
定时任务通过对话创建。告诉 Bot 你想定时做什么，例如：
- 「每天早上 9 点总结我的邮件」
- 「每隔 30 分钟检查一下服务器状态」
- 「明天下午 3 点提醒我开会」

Bot 会自动创建相应的定时任务。

任务连续失败会显示失败计数，可通过查看历史了解详情。禁用后不会丢失，可随时重新启用。
"""

FAQ["channels"] = """\
# 外部通道（飞书 / QQ / 钉钉 / 企业微信 / 微信个人）

## 什么是外部通道
外部通道让你可以通过飞书、QQ、钉钉、企业微信、**微信（个人号）** 等平台和 DeskClaw Bot 对话，无需打开桌面应用。
多数通道使用长连接或流式模式；**微信（个人）** 使用 nanobot 内置的 `weixin` 通道（腾讯 ilinkai HTTPS 长轮询），同样**不需要公网 IP**。

## 配置方式
在飞书/QQ/钉钉/企微等平台创建机器人应用、获取凭证后，可告诉 Bot「帮我配置飞书机器人」等。

**微信（个人）**（须升级到 **CHANGELOG** 配套版本）：**优先在聊天里**让助手按 `read_docs("channels")` 的说明完成登录——由助手拉取 ilink 二维码数据、生成 PNG、用 **`message(media=[图片路径])`** 把二维码发到当前会话，你在窗口内扫码；拿到 **Bot Token** 后写入 **设置 → 通道**（或让助手改配置并重启网关）。不要在回复里只丢一条「去终端执行 nanobot」而没有任何图。

### DeskClaw 下的 nanobot 命令行（备选）
DeskClaw **不会**把 `nanobot` 装进系统 PATH。若你**自行**在终端登录微信，只能用 Gateway 自带 venv，例如：
- macOS / Linux：`~/.deskclaw/gateway-venv/bin/nanobot channels login weixin` 或 `…/python -m nanobot channels login weixin`
- Windows：`%USERPROFILE%\.deskclaw\gateway-venv\Scripts\nanobot.exe channels login weixin`

取得 Token 后在 **设置 → 通道** 粘贴并启用。也可手动改 config.json 的 `channels.weixin`，修改后需重启网关。

---

## 飞书

WebSocket 长连接，支持私聊和群聊。

### 创建步骤
1. 访问飞书开放平台，创建应用
2. 启用「机器人」能力
3. 权限：添加 `im:message`（发送消息）和 `im:message.p2p_msg:readonly`（接收消息）
4. 事件订阅：添加 `im.message.receive_v1`（接收消息）
5. 选择「长连接」模式
6. 在「凭证与基本信息」获取 App ID 和 App Secret
7. 发布应用

### 需要提供的信息
- App ID（cli_ 开头）
- App Secret
- 你的 open_id（首次给机器人发消息后从日志获取）

### 群聊设置
默认需要 @机器人 才会响应。如果想响应所有群消息，可以让 Bot 将 groupPolicy 设为 "open"。

### 飞书 MCP（云文档操作）
配好飞书通道后，还可以接入飞书 MCP，让 Bot 直接读写你的飞书云文档、搜索文档、管理评论等。

**Feishu MCP Proxy（推荐）**：无需内测资格，配好飞书通道后直接让 Bot 帮你配即可——所需的 App ID、App Secret 已经有了。

**飞书官方 MCP（内测中）**：如果你已获得飞书 MCP 内测资格，可以从飞书开放平台获取 MCP URL，让 Bot 帮你配置官方版本。

配好飞书通道后告诉 Bot「帮我接入飞书 MCP」即可。

---

## QQ

botpy SDK，目前支持私聊。

### 创建步骤
1. 访问 QQ 开放平台，注册开发者（个人或企业）
2. 创建机器人应用
3. 进入「开发设置」，复制 AppID 和 AppSecret
4. 沙箱测试：在「沙箱配置」中添加测试成员 QQ 号，扫码开始对话
5. 生产环境：提交审核并发布

### 需要提供的信息
- AppID
- AppSecret
- 你的 openid（首次发消息后从日志获取）

---

## 钉钉

Stream 模式，支持私聊和群聊。

### 创建步骤
1. 访问钉钉开放平台，创建应用
2. 添加「机器人」能力
3. 开启 Stream Mode
4. 添加必要的消息权限
5. 在「凭证信息」获取 AppKey (Client ID) 和 AppSecret (Client Secret)
6. 发布应用

### 需要提供的信息
- AppKey (Client ID)
- AppSecret (Client Secret)
- 你的 staff ID（首次发消息后从日志获取）

---

## 企业微信

WebSocket 长连接，支持私聊。

### 创建步骤
1. 进入企微管理后台 → 智能机器人
2. 创建机器人 → 选择 API 模式（长连接）
3. 复制 Bot ID 和 Secret

### 需要提供的信息
- Bot ID
- Secret
- 你的用户 ID（首次发消息后从日志获取）

---

## 微信（个人）

nanobot 内置 **`weixin`** 通道，走腾讯 **ilinkai** HTTPS 长轮询。**须使用与当前 DeskClaw 版本配套的 nanobot**（见 **CHANGELOG**）。

### 在应用里扫码（推荐）
在对话中说「帮我接微信」等，由助手按内置文档（`read_docs("channels")`）操作：生成登录二维码 **PNG** 并发到聊天里，你在窗口内用微信扫描；完成后将 **Bot Token** 写入通道配置并重启网关。勿依赖终端里看不见的 ASCII 码。

### 在设置里填写
在 **设置 → 通道** 或 **聊天 → 通道** 启用「微信（个人）」，填入 Token（可选 API 地址）；`allowFrom` 可先用 `["*"]`。（可选）高级用户可直接编辑 config.json 的 `channels.weixin`（`baseUrl`、`pollTimeout` 等）。

### 在终端登录（备选）
仅当你习惯命令行时：用 **Gateway venv** 执行 `nanobot channels login weixin`（路径见上文「DeskClaw 下的 nanobot 命令行」），再把 Token 粘到设置页。

### Token 失效
可再在对话里让助手走一遍「发二维码图」流程，或终端重新 `channels login weixin`，更新配置并重启网关。

---

## 常见问题

### 机器人不回复消息
确认通道已启用、allowFrom 包含你的 ID（或 `["*"]`）、应用已发布且权限已授权（飞书/钉钉等）、修改后已重启网关。
**微信（个人）**：重点检查 token 是否有效、是否已保存并重启网关。
详细排查见 `troubleshooting` 主题。

### 怎么获取自己的用户 ID
首次给机器人发消息后，通过「托盘 → 打开日志」在日志中搜索：
飞书 open_id（ou_ 开头）/ QQ openid / 钉钉 staff_id / 企微 user_id；微信个人通道可按日志中的会话标识配置 `allowFrom`。
"""

FAQ["troubleshooting"] = """\
# 常见问题排查

## 「未连接」或「连接中」
- 等待几秒，网关可能正在启动
- 检查设置中的 API 配置是否正确
- 尝试重启应用（托盘 → 退出，然后重新打开）

## 「无 API Key」
- 默认模式：需要登录并激活邀请码
- 自定义模式：需要填写 API 密钥

## 发送消息没有响应
- 检查标题栏连接状态是否显示「已连接」
- 检查 API 配置是否正确（设置 → 测试连接）
- 查看日志：托盘 → 打开日志

## 技能安装失败
- 检查网络连接
- 尝试重新安装（技能库 → 重新安装按钮）
- 查看日志中的具体错误信息

## 启动卡住
- Windows 用户首次启动可能需要较长时间（Python 环境安装）
- 如果超过 5 分钟，尝试关闭后重新启动
- 检查是否有杀毒软件拦截

## 沙箱镜像下载失败
- 确认已安装 Docker 或 Podman
- Windows 用户确认 Docker Desktop / Podman Machine 已启动
- 检查网络连接和代理设置

## 额度用完了
- 默认模式有每日额度限制
- 设置 → 账户标签页查看剩余额度
- 可以切换到自定义模式使用自己的 API

## 飞书/钉钉/QQ 机器人不回复
- 确认 config.json 中 channel 已启用（enabled: true）
- 确认 allowFrom 中包含你的用户 ID（或设为 ["*"]）
- 飞书：确认应用已发布，权限已授权，长连接模式已选择
- 查看日志中是否有报错信息
- 修改 channel 配置后需要重启网关

## 配置改错了无法启动
- DeskClaw 会自动备份成功的配置（config.last-good）
- 连续启动失败 3 次会自动回滚
- 也可以手动将 config.last-good 重命名为 config.json
"""

FAQ["pet"] = """\
# 桌面宠物

## 基本操作
- **点击宠物**: 打开聊天窗口
- **拖拽宠物**: 移动到屏幕上任意位置
- **右键宠物**: 打开菜单

## 右键菜单
- 停下来 / 继续爬: 控制宠物是否自动巡逻
- 跑起来: 宠物兴奋地跑一小会儿
- 打开日志: 打开日志文件夹
- 退出: 关闭整个应用

## 提示
- 宠物会在屏幕边缘自动巡逻
- macOS 上宠物在所有桌面和全屏应用上方都可见
"""

FAQ["personalization"] = """\
# 个性化设置

## 打开个性化
点击左侧栏「个性化设置」。

## 可配置项
- **关于你**: 你的称呼、时区、备注信息（让 Bot 更了解你）
- **Agent 身份**: Bot 的名字、形象、氛围、表情
- **性格与行为**: 性格特征（多选）、回复风格、自定义指令

## 保存
填写完成后点击保存，配置会写入 SOUL 文件，下次对话时 Bot 会按照新设定回复。

需要登录后才能保存。
"""
